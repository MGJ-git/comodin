// Configuración del proyecto de Firebase (Consola de Firebase → Configuración del proyecto → Tus apps → Web).
// Estos valores no son secretos: la seguridad la ponen las reglas de Firestore.
// Mientras sea null, la app funciona en modo "solo este dispositivo".
export const firebaseConfig = null;
